params(["_array"]);

var _stringList = "";
for(var _item : _array) {
    if (_foreachindex != 0) {
        _stringList = _stringList + "," + _item;
    } else {
        _stringList = _stringList + _item;
    }
}

return _stringList;